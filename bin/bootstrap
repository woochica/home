#!/bin/bash

# Start Emacs
emacs --daemon

# Mount encrypted filesystem
if [ ! -f ~/efs/.mounted ] ; then
    /usr/bin/encfs ~/._raw-efs ~/efs
fi

# Run location's bootstrap script
case "$1" in
    "webma") ~/dev/webma/bin/bootstrap;;
esac

# Pull and push
syncrepos
